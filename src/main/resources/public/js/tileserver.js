

window.tileserver = {
    //start()

    websocketCallbacks: [], //[fn(event),...]

    overlays: {}, //name: Layer
    defaultOverlays: [], //Layer

    layers: {}, //name: Layer
    defaultLayers: [] //Layer
};
